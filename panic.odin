package main

main :: proc() {
    // In Odin, panic is a built-in procedure that causes immediate program termination with an error message.
    // It's used for unrecoverable errors.
    panic("a problem")
}
